# cross_diff — 교차검증 verdict

## A-only (B팀이 3-lens 교차판정)

- [Critical] broker/guardrail.py:21,236-254,292-309 (KillSwitch STATE_FILE/roll_day/check_total_drawdown) + reset:220-233 + run_live.py:32-38,123 (make_broker) | 단일 killswitch.json 에 paper $100k 와 toss $21k/$100 baseline·HWM 혼입 → KMI 첫 실거래 false-halt + 수동 JSON 편집 강요 deadlock → **confirm**  lens[confirm/confirm/confirm] reproduce:재현 확인. STATE_FILE=STATE_DIR/'killswitch.json' 단일(21행), _default_state 에 broker 키 없음(173-175). roll_day 가 매 실행 last_equity 영속(253), 새 날이면 base=prior last_equity(247-249). check_total_drawdown 은 hwm=max(hwm,equity) 영속(299-300). make_broker: paper 는 PAPER_CASH 기본 100000(37행), toss 는 cash_cap(35,123행) — 둘 다 동일 STATE_DIR/killswitch.json. 시나리오 재현: paper 실행으로 last_equity=hwm=100000 영속 후 toss 첫 실행 equity≈100 → 새 날 roll_day base=100000. check_daily_loss: dd=100/100000-1=-0.999=-99.9% < -5% → daily_loss 트립(286-289). check_total_drawdown: hwm=max(100000,100)=100000, dd=-99.9% < -20% → total_drawdown 트립(305-308). deadlock 도 재현: total_drawdown 은 새 날 자동해제 대상 아님(resume_if_new_day/roll_day 는 halt_kind=='daily_loss' 한정, 251,263-265), reset()은 total_drawdown 시 hwm=None 만 재seed(224)하고 day_start_equity 는 보존 → reset 후 roll_day 가 day 동일하면 baseline 재설정 안 함, daily_loss 재트립. 운영자가 killswitch.json 수동 편집 없이는 toss 거래 불가. Critical 실재. | refute:STATE_FILE=STATE_DIR/'killswitch.json' (21) 은 브로커 무관 단일 파일이고 _default_state(172-175)에 broker 키 없음 확인. roll_day(247-253)는 prior last_equity 를 baseline 으로 매 실행 영속, check_total_drawdown(298-300)은 hwm=max 영속. paper 는 PAPER_CASH $100k(run_live.py:37), toss 는 cash_cap $100(35,123)로 동일 STATE_DIR 공유 — paths.STATE_DIR 가 USTRADE_HOME/LOCALAPPDATA 단일 경로라 브로커별 분리 없음. paper 선행 실행→last_equity=hwm=100000 영속 시, toss 첫 실행(equity≈$100)에서 roll_day base=100000→check_daily_loss dd=−99.9% trip, check_total_drawdown hwm=max(100000,100)=100000 trip(total_drawdown). reset()은 total_drawdown 일 때 hwm 만 None 재seed(224), day_start_equity(오염 baseline)은 미재seed → reset 후 daily_loss 재트립 deadlock 확인. 자동해제는 daily_loss 한정(251,263-265)이라 total_drawdown 미해제. 반증 실패: paper→toss 순차 실행을 막는 코드 없음(dev→go-live 현실적). cause→consequence 체인 유효. | alt:독립 확인: STATE_FILE 은 단일 killswitch.json(guardrail.py:21), _default_state(172-175)에 broker 키 없음, paths.STATE_DIR 도 단일(브로커 무관). roll_day(253) last_equity 영속, check_total_drawdown(299-300) hwm=max 영속. run_live make_broker: paper=PAPER_CASH 기본 100000(run_live.py:37), toss=cash_cap, 둘 다 같은 STATE_DIR. 검산: paper 선행 실행 후 last_equity=hwm=100000, 토스 첫 실행 equity≈100 → roll_day day_start=prior last_equity=100000(247-249), check_daily_loss dd=100/100000-1=-0.999 < -0.05 → trip(daily_loss) 즉시 false-halt 실재. benign 해석 없음(브로커별 네임스페이스·스케일점프 재seed 부재). 단 코드순서상 check_daily_loss(204)가 check_total_drawdown(205)보다 먼저 HaltError raise → 단일 런에선 total_drawdown 트립까지 도달 안 함(daily_loss 는 새 날 자동해제 대상). 그래도 paper/toss 오염→첫 실거래 false-halt 핵심은 실재. cause→consequence 병합 정당.
- [Critical] run_exit.py:93-96 + broker/toss.py:208-215 get_quote + live_exit.py:52,60-65 check_exits + broker/guardrail.py:388-392 | 토스 lastPrice 결측 시 get_quote 가 0.0 반환 → 관리종목 거짓 청산(시장가 강제 매도) → **confirm**  lens[confirm/confirm/confirm] reproduce:재현 확인. TossBroker.get_quote: res 비어있으면 raise(211-212)하나 res[0] 존재 & lastPrice 가 None/누락이면 last=_num(res[0].get('lastPrice'))=_num(None)=0.0(213,_num 65-66 None→default 0.0) 반환, 예외 없음. run_exit._run_locked: live[s]=gbroker.get_quote(s).last, except 는 raise 시에만 None(93-96) → 0.0 은 통과. check_exits: 가드가 `if price is None or s is None or len(s)<regime_ma`(52)로 price=0.0 미차단 → sma200>0 이므로 0.0<sma200 '200MA 이탈'(60-61) + avg_price>0 이면 0.0<avg_price*(1-stop) '손절'(63-64) 트리거, data_ok=True 유지 → to_exit(80)가 자동청산 대상. GuardedBroker.place_order: bad price 거부는 BUY 한정(391), SELL 은 허용(389-394 else 분기로 notional 체크만) → 빈 호가 한 번에 봇 보유분 시장가 강제 매도. Critical 실재. | refute:get_quote(213) last=_num(res[0].get('lastPrice')), _num(None)=0.0(65-66). line 211 은 res 빈 경우만 raise — res[0] 존재하나 lastPrice 키 부재/null 이면 예외 없이 last=0.0. run_exit(94) live[s]=get_quote(s).last, except(95-96)는 raise 시만 None — 0.0 통과. check_exits(52)는 price is None/s is None/len 만 거름, price>0 미검사 → 0.0 통과. 60-62 price<sma200(0.0<sma200 참)+63-65 price<avg*(1-stop)(참) 동시 트리거, data_ok=True → to_exit 포함. GuardedBroker(388-392)는 SELL bad-price 허용(391은 BUY 만 거부). executor.py:39 는 price<=0 raise 로 막으나 run_exit/check_exits 경로엔 그 가드 부재 — 별개 진입점 비대칭 확인. 반증 실패: 빈 lastPrice 한 번이 봇 보유분 시장가 강제청산으로 직결, 가드 공백 실재. | alt:toss.py:213 last=_num(res[0].get('lastPrice')), _num(None)=0.0(63-70) — res[0] 존재하나 lastPrice null/누락 시 예외 없이 0.0 반환(no-price 예외는 res 빈 리스트일 때만:211). run_exit.py:94 except 는 raise 시에만 None(96), 0.0 통과. live_exit.py:52 가드 `if price is None or s is None or len(s)<regime_ma` — price>0 검사 없음 → price=0.0 통과. 60-62 0.0<sma200 트리거, 63-65 0.0<avg*(1-stop) 트리거 → to_exit(80) 자동청산. guardrail.py:391-392 SELL 은 bad price 허용(BUY 만 거부) → 봇 보유분 시장가 강제 청산 경로 실재. benign 해석 없음 — run_exit/check_exits 경로엔 executor.py:39-40 같은 price<=0 raise 가드 부재.
- [Critical] live_engine.py:_await_fills(63-85)·_run_once_locked(229-245) + broker/managed.py:reconcile_basis(234-257) + broker/toss.py:243-265(SUBMITTED·DAY tif:251); grep: live 경로 cancel_order 0건 | 미체결 잔존 DAY주문 미취소 → 다음 런 재플랜으로 더블바이(KMI 1주 검증서도 발생 가능) → **partial**  lens[confirm] refute:_await_fills(68-85)는 폴링만, cancel 호출 없음. live_engine/run_exit/run_live 어디에도 live 경로 cancel_order 호출 없음 확인(managed.py:150-151 은 정의 passthrough 일 뿐 미호출). 미체결 시 _run_once_locked 는 partial 반환(231-239), mark_traded 는 ok 경로(245)만 → already_traded() False. toss 주문 DAY(251) 장중 잔존. 다음 런 reconcile_basis 는 get_positions(체결분만, 열린주문 미반영, 245) 기반 → cur_qty=0 으로 재플랜 diff 양수 → 2차 BUY. 두 주문 모두 체결 시 2배. 반증 실패: pending/reconcile 는 실보유 cap 이라 미체결(실보유0)을 흡수 못 하고 재매수 막지 못함 — 더블바이 메커니즘 유효.
- [Critical] broker/toss.py:place_order(258-263)·_request(116-151) 5xx/타임아웃 raise 경로 + live_engine.run_once(200-216)·_reconcile(88-108); SELL pending 미보호 | 주문 응답 유실(타임아웃/5xx) 시 체결된 주문을 미체결로 오인 → order_id 소실·유령포지션, 특히 SELL 청산 → **partial**  lens[confirm] refute:체결됐으나 응답유실(timeout→network-error 124, 접수後 502/503→5xx raise 137-139,151) 시 place_order 가 is_business_error()==False(60)라 re-raise(263). run_once plan 루프(208-209)가 raise 로 빠져 Order 미append → order_id 봇기록 소실. BUY 는 pending(managed.py:198)+reconcile_basis 가 실보유 대조로 일부 흡수하나, SELL 청산은 pending 보호 부재(managed.py:226 fq>0 일 때만 basis 차감, 제출 前 영속 없음) → 체결된 매도 미기록 시 봇이 보유로 오인 → run_exit 재매도/run_live 잘못된 prior_qty. _reconcile(88-108)은 drift 보고만 복구 없음. 반증 실패: BUY/SELL 비대칭(SELL breadcrumb 부재)과 응답유실 복구경로 부재 실재.
- [Critical] broker/toss.py:place_order(247)·get_quote(210)·cancel/get_order 진입부 — 아웃바운드 심볼 미정규화 + managed.py:188-200(s=_norm 검사하나 원본 req 전달) | 내부→토스 아웃바운드 심볼이 역정규화 안 됨 — 점 표기 클래스주(BRK-B/BRK.B) 계약 불일치로 sp100 운영 시 매수·시세 전량 실패 → **partial**  lens[partial] refute:코드 관찰은 정확: place_order(247) body['symbol']=req.symbol, get_quote(210) params={'symbols':symbol} 모두 역정규화 없음. 그러나 Critical 결론은 '토스가 점표기(BRK.B)를 기대한다'는 미검증 전제에 의존 — 이 루트 코드 어디에도 토스 기대표기 증거 없고, finding 자체가 '토스가...받으면' 조건부이며 오늘 KMI 는 무해함을 인정. managed sub-claim(원본 req 전달, 200): place_order 가 _norm 으로 가드만 비교 후 원본 req 전달은 사실이나, 라이브 경로 입력심볼은 이미 canonical(get_positions 가 _norm 노출 165, weights 키=유니버스 하이픈)이라 'googl' 같은 비정규화 전달 시나리오가 실제 경로서 발생 안 함 — 디버전스 없음. 즉 코드사실은 confirm 이나 자금손실 consequence 는 conditional/unverified — partial.
- [Critical] broker/toss.py:place_order(247) clientOrderId — uuid 매 호출 새 생성 | 멱등키가 호출마다 새로 생성 → 논리적 재시도(재플랜·운영자 재실행) 중복접수 못 막음 → **partial**  lens[confirm] refute:clientOrderId=uuid.uuid4().hex[:32](247)가 매 place_order 호출마다 새로 생성, 영속/재사용 로직 없음 확인. 동일 논리주문 상위 재시도(재플랜·운영자 재실행)는 새 uuid → 토스가 별개 접수, 비즈니스레벨 중복 미차단(주석의 '재요청 안전' 주장과 불일치). _request(133-139)는 method 무관 429/5xx 재시도 — POST 도 대상, 단 body 가 루프 前 고정이라 동일콜 내 재전송은 같은 uuid(HTTP 레벨 멱등은 토스 서버측 honor 가정 시만). 핵심결함(uuid per-call 이 논리 멱등 무력화) 코드로 검증됨. 반증 실패.
- [Major] run_live.py:104-122(sleeve_protected drop, toss 분기) + live_engine._run_once_locked(169-173) 보호가드 부재 | 보호종목 후보-제외가 run_live 단일 호출 지점에만 존재 — 엔진 계층엔 강제 안 됨 → **partial**  lens[confirm] refute:sleeve_protected 채움·drop 은 run_live broker_kind=='toss' 분기(104-113,119-122)에만 존재. _run_once_locked(171-173)는 prices 를 sel_fn 에 그대로 흘리며 protected 제외 안 함 확인. 엔진계층 backstop 은 ManagedBroker BUY 가드(192-194)뿐. 신규/직접 진입점이 사전-drop 빠뜨리면 AMAT(sp100 멤버 universe.py:23, 보호종목)이 모멘텀 게이트 통과해 weights 진입 가능→Executor.plan 예산배정 후 managed 거부로 매수예산 잠식. 반증: 오늘은 run_live 가 drop 수행, BUY 가드가 보호위반은 막음 — 그러나 '엔진경계 미강제' 아키텍처 관찰 자체는 정확하고 단일호출지점 의존은 실질 방어층 공백. confirm(Major, 영향은 backstop 으로 제한적이나 결함 실재).
- [Major] broker/executor.py:26-30(getattr buy_mult)·38-41,62-68(last 기반 사이징) + managed.py/toss.py(_commission·_spread·_slippage 부재) | 토스 라이브에서 buy_mult=1.0 + MARKET 을 last 평탄시세로 사이징 — 수수료/슬리피지 버퍼 0, 체결가>last 시 cash_cap 초과 → **(무)**  
- [Major] broker/guardrail.py:RunLock(83-160)·LOCK_FILE(22) + run_exit.py:55·live_engine.py:126 공유 락 + pid 재사용 회수(104-124) | run_live·run_exit 가 단일 run.lock 공유 — 크래시 락 잔존/pid 재사용이 청산을 최대 1~6h 무경보 차단 → **partial**  lens[confirm] refute:LOCK_FILE=STATE_DIR/'run.lock'(22) 단일 default, run_live(live_engine 126)·run_exit(55) 모두 RunLock() 기본락 공유 확인. run_live 크래시로 락 잔존 시 heartbeat 사망→age 누적, age<=_LOCK_STALE_SEC(1h) 동안 회수 안 됨(112)→run_exit 모든 호출 'locked' 반환, run_exit.py:149 benign(exit0) 무알림 → 손절/200MA 청산 최대 1h 무경보 미작동. Windows pid 재사용 시 1~6h 구간 _pid_alive True 오판(55-60)으로 6h 까지 미회수. 반증: 살아있는 장기실행은 heartbeat 가 정상 보호 — 그러나 finding 은 크래시 시나리오라 무효. 무경보 청산차단 실재. confirm.
- [Major] broker/toss.py:get_account(183-192)·_num(63-70) + guardrail.py:272·280-281·302-303·328-330 | 토스 API 가 equity=0.0 으로 강제 coerce 되면 첫 실행 양 손실가드 fail-open + 비례 명목캡 비활성 → **partial**  lens[confirm] refute:get_account(187-191) cash=_num(bp.get('cashBuyingPower')), pos_val=_num(amount.get(key)) — .get('result') or {} 패턴서 손상/빈 envelope 면 _num(None)=0.0→equity=0.0. _require_finite_equity(272)는 isfinite 만 보므로 0.0 통과. 첫 실행 check_daily_loss base None→0.0 반환(280-281), check_total_drawdown hwm=max(None→0,0)=0,'if hwm<=0:return'(302-303) 통과 → 양 손실가드 무력화. run_equity=0.0→check_order_notional 'if run_equity and >0'(328) False→비례캡 비활성, 절대 $1M 캡만. 반증 실패: base<=0 은 fail-closed(282-283) 대칭이 equity<=0 에는 부재 — fail-open 비대칭 실재. 손상응답 0 coerce 가 가드 우회.
- [Major] broker/toss.py:_ensure_connected(153-156)·_request 4xx(137-150)·place_order(261) | 토큰 조기 만료(401)를 비즈니스거부로 오인 → 유효주문 REJECTED, 재인증 안 함 → **partial**  lens[confirm] refute:_ensure_connected(154-155)는 로컬 monotonic vs _token_expiry 만 비교 — 서버측 조기무효(키회전/재시작/스큐) 미감지. 토스 401 은 4xx 라 재시도(429/5xx 한정 133-139) 대상 아님→즉시 raise(149-150). place_order 에서 is_business_error()==True(60:400≤s<500)→REJECTED 반환(261-262). 재인증 로직 부재 확인. 유효주문이 영구거부, get_account/get_positions/get_quote 도 401 시 raise→run_once error/partial 종료, 재인증 시도 없음. 반증 실패: 401 재인증·transient 분류 경로 코드상 부재, false-negative 실재.
- [Major] broker/toss.py:_STATUS_MAP(34-45)·get_order(279) + live_engine._await_fills(63-85) | 상태매핑 누락값(접수대기/EXPIRED/오타스펠)→SUBMITTED 폴백으로 미체결 오인·무한폴링·다음 런 중복 → **partial**  lens[confirm] refute:_STATUS_MAP(34-45)는 PENDING/PARTIAL_FILLED/FILLED/CANCELED/REJECTED 등만, get_order(279) 미지값→OrderStatus.SUBMITTED 폴백 확인. 토스 실응답이 NEW/ACCEPTED/EXPIRED/DONE_FOR_DAY 또는 PARTIALLY_FILLED(map 의 'PARTIAL_FILLED' 와 스펠 상이)면 종결상태가 SUBMITTED 로 남아 _await_fills 30s 무의미폴링 후 비종결→partial(231-239)·record_error 누적. EXPIRED/DONE_FOR_DAY 가 CANCELLED 미매핑→봇이 살아있는 주문 오인→다음 런 중복. 반증: 토스 enum 실값은 코드로 미검증이나 '미지→SUBMITTED 폴백'(경고/드리프트 아님)과 PARTIAL_FILLED 스펠 위험은 코드로 검증되는 방어결함. confirm.
- [Major] data.py:90-111 load_panel + live_engine.py:151-157(staleness 게이트) + live_select_canslim.py:53-62 / live_risk.py:23-32 | 패널 staleness 게이트가 최신 티커 하나로 우회 + SPY ffill stale → 종목별/레짐 거짓 신호 → **confirm**  lens[confirm/confirm] reproduce:재현 확인. load_panel: panel=pd.DataFrame(closes).dropna(how='all')(110) — union 인덱스, 한 종목만 최신봉이어도 panel.index[-1] 가 그 날짜. live_engine H1 게이트: last_bar=prices.index[-1], gap=session_gap(last_bar,today)(152-154) — 패널 max 인덱스만 판정 → 대다수 stale 이어도 단일 종목 신선하면 통과. select(canslim): 종목별 s=prices[t].dropna()(54)의 자기 iloc[-1]/rolling200/rolling252/_mom_12_1 사용(58-62) → stale 종목은 오래된 마지막봉으로 신호 계산(stale-back). live_risk.apply_overlay: spy=data.load(...)['Close'].reindex(prices.index).ffill()(25) — yfinance 가 SPY 1세션 짧게 주면 패널 최신날짜 NaN→ffill 이 직전 stale SPY 종가 채움, px=spy.iloc[-1](27) stale, isfinite 체크(28)는 ffill 후라 통과 → risk_on=px>mav(32) 경계서 뒤집힘. Major 실재. | refute:load_panel(110) pd.DataFrame(closes).dropna(how='all') union 인덱스 → 한 종목만 최신이어도 panel.index[-1] 가 그 날짜. live_engine H1 게이트(152-154)는 prices.index[-1] 만 판정 → 대다수 stale 이어도 단 한 종목 신선하면 통과 확인. select(canslim:53-64)는 종목별 s=prices[t].dropna().iloc[-1] 써 stale 종목은 옛 마지막봉으로 신호 계산(stale-back). live_risk(25) spy.reindex(prices.index).ffill() → SPY 1세션 짧으면 패널 최신날짜 NaN→ffill 이 stale SPY 종가 채움, px=spy.iloc[-1](27) stale, isfinite(28)는 ffill 後라 통과 → 레짐이 1세션 늦은 SPY 로 결정. 반증 실패: 패널 max 게이트가 종목별 staleness 못 잡고, isfinite 가 ffill 후라 stale 은폐 — 두 메커니즘 코드로 검증.
- [Major] run_exit.py:86-99 — 청산 경로 데이터 신선도 게이트 부재 | 청산(run_exit)엔 stale 데이터 차단 게이트가 없음 — 진입(run_live)과 비대칭 → **partial**  lens[confirm] refute:live_engine.run_once 는 max_staleness_sessions 게이트(150-157) 보유. run_exit._run_locked(86-99)에 session_gap 호출 전무 확인 — closes[s]=data.load(...)['Close'].dropna() 후 check_exits 가 len>=200(live_exit.py:52)만 만족하면 SMA/RSI 산출, 지연 실시간가와 비교해 트리거. 200MA 는 1세션 stale 로 거의 불변이나 RSI 과열트림/50MA/손절은 stale 일봉+지연가 조합으로 거짓신호 가능. 진입은 보호·청산은 무방비 비대칭 실재. 반증 실패: calendar_util.session_gap 은 존재하나 run_exit 가 미사용.
- [Minor] toss_setup.py:52-61 + broker/toss.py:194-206(marketCountry=='US' 필터) | protected 스냅샷이 US-only 보유에서만 생성 — 비US/미분류 보유분 보호 누락 가능 → **confirm**  lens[confirm/confirm/confirm] reproduce:재현 확인. toss_setup: holdings 를 b.get_positions() 결과로만 구성(52-53), protected=set(holdings)-managed-pending(61). TossBroker.get_positions 는 marketCountry==self._market_country('US') 항목만 반환(201-202). 보호 종목이 모두 US 면 정상이나, (1)토스가 marketCountry 누락/오분류 또는 (2)펀딩 직후 holdings 일시 빈/부분이면 그 종목이 protected 에서 빠짐. 사용자 보유 11종목(CONL/BITX/TSLL/AMDL/PLTU/MUU/IONX/CWVX/SNXX/VOO/AMAT)이 marketCountry 누락 시 protected 미포함 → 후보-제외도 안 됨(run_live drop 은 protected 기준). 스냅샷 시점 holdings 신뢰도에 보호 경계 전체가 의존. Minor 실재. | refute:toss_setup(52-61) protected 를 b.get_positions() 결과에서만 생성. TossBroker.get_positions(201) marketCountry!=self._market_country continue — US 항목만 반환 확인. 토스가 marketCountry 누락/오분류하거나 펀딩직후 holdings 일시 빈/부분이면 그 종목이 protected 에서 누락 → 후보-제외도 안 됨(protected 부재). 스냅샷 시점 holdings 신뢰도에 보호경계 의존. 반증: 사용자 11종목 전부 US 면 정상이나, marketCountry 결손 시 보호공백은 실재 — holdings 검증단계 부재. Minor 결함 confirm. | alt:toss_setup.py:52-53 protected 를 b.get_positions() 결과로만 생성, 61 protected=set(holdings)-managed-pending. toss.py:201 get_positions 는 marketCountry!='US' continue → US-only 반환 직접 확인. 보호종목이 모두 US 면 정상이나 (1)토스 marketCountry 누락/오분류 또는 (2)펀딩 직후 holdings 빈/부분이면 그 종목 protected 누락→봇이 매수·매도 후보로 취급. 스냅샷 시점 holdings 신뢰도에 보호경계 의존하는 결함 실재. holdings 검증(기대수 대비 부족 시 거부) 부재 확인. Minor 타당.
- [Minor] broker/executor.py:25,60(investable=equity*alloc vs budget=cash+proceeds) + managed.py:175-184(get_account) | cash_cap 은 현금배포 캡일 뿐 — managed 평가액 상승분이 노출을 캡 초과로 키움 → **(무)**  
- [Minor] broker/executor.py:41(tgt_qty=int(w*investable/price)) + live_select_canslim.py:98(등비중) + broker/toss.py:250(int 절사) | KMI 첫 거래가 선정 종목수에 따라 0주로 floor + 분수 basis 청산 시 0주 매도 가능 → **(무)**  
- [Minor] broker/toss.py:cancel_order(267-273) | 취소 transient 오류(5xx/네트워크/타임아웃)를 False(취소실패)로 뭉개 상태 오인 → **partial**  lens[confirm] refute:cancel_order(269-273)가 모든 TossAPIError 를 except→False 반환 확인. 4xx(이미체결409/잘못된id)와 5xx/네트워크타임아웃 미구분 — 토스가 취소 접수·처리했으나 응답유실(timeout)돼도 False(미취소) 보고. 호출측이 '주문 살아있다'로 해석 시 desync. place_order(258-263)는 is_business_error() 분기하나 cancel 은 미구분 — 계약 일관성 깨짐. 반증: 현재 cancel 직접호출 경로 좁으나(Minor), transient/business 미구분 결함 자체는 코드로 검증됨. confirm(Minor).
- [Minor] broker/toss.py:market_open(217-235) + run_exit.py:70-71 / calendar_util.py 폴백(50-52) | 장 캘린더 tz 가정 불명확 → 항상 closed 오판으로 청산 영구 skip + 청산 today 폴백 세션 불일치 → **partial**  lens[confirm] refute:market_open(230-232) fromisoformat 파싱 후 now=datetime.now().astimezone()(tz-aware) 비교 — 토스가 tz 오프셋 없는 naive 윈도 주면 aware↔naive TypeError→except(234)→False(휴장)→run_exit(70-71) 영구 skip(closed)→손절/200MA 청산 미작동. fail→closed 라 손실직결은 아니나 청산가드 silent 무력화. run_exit.run(53) today=(session or datetime.now().date()) — last_completed_session None 시 KST now.date 폴백→US 세션 어긋남, KillSwitch baseline/day 키 오날짜. run_live(96-98)는 session None 명시차단인데 run_exit 는 조용히 폴백 비대칭 확인. 반증 실패: tz·폴백 두 silent 오작동 실재.
- [Minor] broker/managed.py:place_order(195-200) pending 영속 위치 vs RunLock 임계구역 | pending 영속 후 주문 직전 크래시 시 pending 과대(유령은 실보유 cap 으로 차단) → **partial**  lens[confirm] refute:BUY pending 영속(_save 199)이 self._pending 갱신 후 디스크 원자저장. place_order 와 실제 broker.place_order(200) 사이 크래시 시 pending +qty 됐는데 주문 미제출. 다음 reconcile_basis(250) min(실보유,basis+pending) 로 실보유 cap → 유령포지션 안 생김(실보유0→pop). 단 pending 과대계상이 그 종목 reconcile 까지 1런 지연 가능 — finding 도 '긴급 아님' 자평. 반증: 실보유 cap 이 자금손실/유령 차단함을 finding 스스로 인정, 영속시점 order_id 미발급 추적불가는 breadcrumb 도입 시 해소 — 관찰은 정확하고 결함(과대계상 1런지연)은 미세하나 실재. confirm(Info/Minor).
- [Info] broker/managed.py:154-184(get_positions/get_account)·203-257(record_fills/reconcile_basis) + guardrail.py:184(buying_power=cash)·328-334(비례 명목캡) | 보호 격리·co-mingle 흡수 차단·buying_power 캡·sleeve 기준 명목캡 — 공격벡터 b/c/d 부재 확인(안전) → **(무)**  
- [Info] live_select_canslim.py:26-31(_A_DIR sibling import)·34-36(_mom_12_1)·55(길이게이트) + live_engine.py:19-23,169-170 + run_live.py:103-123(protected drop 순서) | canslim 형제프로젝트 import 단일실패점 + 인덱싱 룩어헤드 없음 + 보호 drop 순서 정합(확인, 일부 통합위험) → **(무)**  

## B-only (A팀이 3-lens 교차판정)

- [Major] broker/executor.py:26-29, 60-68 (Executor.plan, buy_mult / budget headroom) | 라이브 toss 경로에서 비용버퍼(buy_mult)가 1.0 — EXEC-1 보호 무력 → **confirm**  lens[confirm/confirm/confirm] reproduce:재현됨. executor.py:26-28 은 getattr(self.broker, "_commission"/"_spread"/"_slippage", 0.0) 로 읽는다. 라이브 self.broker 는 GuardedBroker→ManagedBroker→TossBroker. GuardedBroker.__getattr__(guardrail.py:397-402)는 _inner(ManagedBroker)로 위임하나 ManagedBroker(managed.py:106-264)는 __getattr__ 가 없고 명시 패스스루(137-151)만 있으며 _commission 미정의. TossBroker(toss.py)도 미정의. _commission 정의는 paper.py:24-26 한 곳뿐(grep 확인). 따라서 셋 다 default 0.0 → buy_mult=(1+0/2)(1+0)(1+0)=1.0. 라이브 매수 unit=price 로 헤드룸 0. docstring(executor.py:19-22)의 EXEC-1 보호가 라이브에서만 무력. 검산 1.0 확인. | refute:반증 실패 — 결함 실재. 라이브에서 Executor 에 주입되는 broker 는 GuardedBroker(gbroker, live_engine.py:198-199)다. getattr(gbroker,'_commission',0.0) 은 GuardedBroker.__getattr__(guardrail.py:397-402)으로 _inner=ManagedBroker 에 위임되는데, ManagedBroker 는 _commission/_spread/_slippage 속성도 __getattr__ 위임도 없다(managed.py 전체 — 명시 패스스루 connect/disconnect/get_quote/get_order/cancel_order/get_positions/get_account/place_order/record_fills/reconcile_basis 만 존재, _broker 로의 속성 위임 부재). 따라서 셋 다 AttributeError→default 0.0. 검산 buy_mult=(1+0/2)(1+0)(1+0)=1.0 확인. paper.py:24-26 만이 이 세 속성을 정의(PaperBroker). 즉 EXEC-1 비용버퍼가 라이브 전 경로에서 구조적으로 0 — docstring(executor.py:19-22)이 명시한 보호가 라이브에서만 부재. 단 본문도 인정하듯 cash_cap=$100·KMI 소수주 단계에선 실해는 cents 수준(즉시 사고 아님) — 결함은 실재하나 영향은 경미. 정합성 결함 confirm. | alt:체인 추적 확인: live 경로의 Executor 는 gbroker=GuardedBroker(broker=ManagedBroker(TossBroker)) 를 받는다(live_engine.py:198-199). getattr(self.broker,'_commission',0.0) → GuardedBroker.__getattr__(guardrail.py:397-402)가 _inner(ManagedBroker)로 위임 → ManagedBroker 는 _commission/_spread/_slippage 도 __getattr__ 도 없음(managed.py 전체 명시 메서드만, 위임 부재) → AttributeError → default 0.0. 따라서 buy_mult=(1+0/2)(1+0)(1+0)=1.0(검산 일치). _commission 정의는 paper.py:19,24-26 한 곳뿐. docstring(executor.py:19-22 EXEC-1)이 약속한 비용 헤드룸이 라이브에서만 0 으로 무력화됨이 실재. benign 해석(실해 미미)은 결함 '실재' 자체를 부정 못 함 — cash_cap $100·KMI 소수주에선 초과가 cents 수준이라 즉시사고는 아니나 명시 안전마진 부재는 사실이며 finding 도 동일하게 한정함. 결함 실재.
- [Major] run_exit.py:_run_locked (107-124) + broker/managed.py:place_order(187-200)·record_fills(226-227) | 청산 SELL은 멱등키·pending 없어 늦은체결 시 다음 cron이 같은 주식 재매도(oversell) → **confirm**  lens[confirm/confirm/confirm] reproduce:재현됨. run_exit.py:109-111 은 exits 루프에서 무조건 gbroker.place_order(SELL) 호출, mark_traded/already_traded 부재(run_exit.py 전체에 없음). managed.py:189 SELL 가드는 's in self._managed'만 검사하고 진행 중 매도 의도 미검사. managed.py:195-199 는 BUY 만 _pending 영속, SELL 은 미영속. record_fills SELL 분기(managed.py:226-227)는 'fq>0' 일 때만 managed 차감 → SUBMITTED(fq=0)면 managed 불변. live_engine.py:60 _TERMINAL={FILLED,REJECTED,CANCELLED} 로 SUBMITTED 제외 → _await_fills(30s, run_exit.py:118) 타임아웃 후에도 get_positions(managed.py:154-166)가 동일 수량 노출 → 다음 15분 cron 의 check_exits 가 같은 트리거로 동일 SELL 재제출. 두 SELL 이 토스에 동시 열려 oversell. 모든 라인 근거 일치. | refute:반증 실패 — 결함 실재. run_exit.py 전체에 mark_traded/already_traded 호출 없음(멱등 보호 부재 확인). SELL 경로 멱등 부재 확인: managed.py:195-199 는 BUY 만 _pending 영속, SELL 은 가드(189: s in managed & ¬protected)만 통과시키고 의도 영속 없음. record_fills SELL 분기(226-227)는 fq>0 일 때만 managed 차감 — SUBMITTED(0 체결)면 managed 불변. live_engine.py:60·_TERMINAL 은 PARTIAL/SUBMITTED 제외이므로 30s 타임아웃(run_exit.py:118)에 미체결 SELL 은 SUBMITTED 잔존 → managed 유지 → 다음 15분 실행 get_positions 가 동일 수량 노출 → check_exits 동일 트리거 → place_order SELL 재제출(run_exit.py:111). RunLock 은 동시 실행만 막을 뿐 15분 간격 순차 재제출은 못 막음(매 실행 락 해제). 따라서 동일 3주에 두 건 SELL 이 토스에 열려 oversell 위험 실재. (실제 oversell 의 최종 차단은 토스 서버측 보유수량 거부에 의존 — 봇 레벨엔 SELL outstanding 추적 부재가 root.) confirm. | alt:재현 경로 확인: run_exit.py 에 mark_traded/already_traded 호출 전무(멱등키 부재), 라인 110-111 은 exits 후보마다 무조건 gbroker.place_order(SELL). managed.py place_order 는 BUY 만 _pending 영속(195-199), SELL 은 통과만(가드 라인 189 는 's in self._managed' 만 확인, 진행중 매도의도 미검사). record_fills SELL 분기(226-227)는 fq 만큼만 managed 차감, dedup/pending 마커 없음. _await_fills 타임아웃 30s(run_exit.py:118), _TERMINAL={FILLED,REJECTED,CANCELLED}(live_engine.py:60)로 PARTIAL/SUBMITTED 제외 → SUBMITTED(0체결) SELL 은 record_fills 가 managed 미차감, 토스 holdings 미정산 → 다음 15분 실행 get_positions(managed.py:154-166)가 동일 basis 노출 → check_exits 동일 트리거 재발화 → 같은 심볼·수량 SELL 재제출. 봇측 보유수량 가드 부재(PaperBroker 의 pos.qty<req.qty 거부에 해당하는 검사가 라이브 SELL 경로엔 없음). benign(다음 reconcile 가 흡수)은 SELL 엔 적용 안 됨(pending 은 BUY 전용). oversell 위험 실재.
- [Major] broker/guardrail.py:21 STATE_FILE / :175,248,253 roll_day·last_equity / :299 check_total_drawdown·hwm ↔ run_live.py:28-38 make_broker (paper cash=100000 vs toss cash_cap=100) | 단일 killswitch.json 을 paper·toss 가 공유 → baseline·hwm 스케일 혼입 오염 → **confirm**  lens[confirm/confirm/confirm] reproduce:재현됨. guardrail.py:21 STATE_FILE=STATE_DIR/'killswitch.json' 단일, broker 모드 키잉 없음. run_live.py:36-38 paper cash 기본 100000, :35/managed.py:177 toss cash_cap. 두 경로 모두 live_engine.py:203-205 roll_day·check_total_drawdown 을 같은 ks 에 기록. roll_day(guardrail.py:248) day_start_equity=last_equity 이월, :253 last_equity=equity 저장. check_total_drawdown(:299) hwm=max(hwm,equity) 단조영속. paper $100k 오염 후 toss equity≈$132 → check_daily_loss dd=132/100000-1≈-0.99868(-99.9%)<-5% 트립, check_total_drawdown dd≈-99.9%<-20% 트립. total_drawdown 은 roll_day 자동해제 대상 아님(guardrail.py:251 daily_loss 만), reset(:224-226)에서만 hwm 재seed → 수동 reset 전까지 영구정지. paths.py:26-29 STATE_DIR 모드무관 단일 확인. 검산 일치. | refute:반증 실패 — 결함 실재. STATE_FILE=STATE_DIR/'killswitch.json'(guardrail.py:21) 단일·broker 모드 키잉 전무. paths.py:16-27 STATE_DIR 모드 무관 단일. roll_day 가 새 날 baseline=last_equity 이월(guardrail.py:247-249), 매 실행 last_equity=equity 저장(253), check_total_drawdown hwm=max(hwm,equity) 단조영속(299). run_live.make_broker paper 는 PaperBroker(cash 기본 100000, run_live.py:37), toss 는 cash_cap~100. 두 경로 모두 live_engine.run_once→roll_day/check_total_drawdown 동일 ks 사용(live_engine.py:203-205). paper 실행이 last_equity·hwm 을 100000 으로 오염 후 toss 실행(equity≈132) 시 dd=132/100000-1≈-0.99868≈-99.9% 검산 확인 → check_daily_loss(<-5%)·check_total_drawdown(<-20%) 즉시 트립. total_drawdown 트립은 roll_day/resume_if_new_day 자동해제 대상 아님(daily_loss 만, guardrail.py:251,264)·reset 시에만 hwm 재seed(225) → 수동개입 전까지 영구정지. 시나리오는 동일 USTRADE_HOME 에서 paper 실행이 선행해야 성립(개발 중 충분히 발생가능). confirm. | alt:단일 STATE_FILE=STATE_DIR/'killswitch.json'(guardrail.py:21), broker 모드 키잉 전무. paths.py:16-27 STATE_DIR 은 USTRADE_HOME 단일·모드무관. PaperBroker 기본 cash=100000(run_live.py:37), toss 는 cash_cap. 양 경로 모두 roll_day(acct.equity)·check_total_drawdown(live_engine.py:203-205)으로 같은 state 기록. roll_day baseline=last_equity 이월(guardrail.py:247-249), 매 실행 last_equity=equity 저장(253), check_total_drawdown hwm=max(hwm,equity) 단조영속(299). total_drawdown 트립은 새 날 자동해제 대상 아님(roll_day 251·resume_if_new_day 263-265 모두 daily_loss 만) → 수동 reset 전까지 영구정지. 검산 132/100000-1≈-0.99868≈-99.9% 일치(check_daily_loss -5%, check_total_drawdown -20% 모두 즉시트립). benign(동일 USTRADE_HOME 안 쓰면 안전)은 finding 도 인정하나 기본값이 동일 경로라 오염 가능성 실재. 결함 실재.
- [Major] broker/toss.py:116-124, place_order via _request (243-263) | 네트워크 타임아웃이 미체결 단정으로 raise + clientOrderId 매 호출 uuid → 재시도 시 더블체결 위험 → **confirm**  lens[confirm/partial/confirm] reproduce:재현됨. toss.py:120-124 RequestException(읽기/연결 타임아웃)은 max_retries 소진 후 TossAPIError("network-error",...,None)(http_status=None) raise. is_business_error()(toss.py:59-60)는 http_status is not None 조건이라 None→False → place_order(toss.py:261-263)에서 raise 경로(REJECTED 흡수 안 됨). 읽기타임아웃은 서버 접수·체결됐으나 응답만 못 받은 상태 가능. live_engine.py:208-216 raise 시 해당 주문 orders 미기록·record_error → 다음 실행 동일 비중 재plan. clientOrderId=uuid.uuid4().hex[:32](toss.py:246)는 매 place_order 호출마다 새 키 → 재시도 시 별개 주문으로 더블체결 위험. _request 내부 재시도는 같은 body 라 멱등하나 place_order 재호출 경로는 비멱등. 근거 일치. | refute:사실관계 confirm, 결과(더블체결) 는 BUY 경로서 반증됨. 사실: toss.py:124 network-error 를 http_status=None 으로 raise, toss.py:261-263 은 is_business_error()(4xx)만 REJECTED 흡수·그 외 raise 이므로 network-error 는 raise 경로, toss.py:246 clientOrderId=uuid.uuid4() 매 place_order 호출마다 새 키 — 모두 확인. _request 내부 재시도는 동일 body(동일 uuid) 재사용이라 멱등. 그러나 '다음 실행에서 동일 비중 재plan → 더블체결' 인과는 BUY 에서 깨진다: managed.py:198-199 가 submit '前' pending 을 영속하고, live_engine.py:161-166 이 다음 실행 시작에 reconcile_basis 를 먼저 호출 → 타임아웃된 BUY 가 실제 체결됐으면 real 보유에 잡혀 basis=min(real,basis+pending) 로 흡수 → Executor diff≈0 → 재매수 안 함. 미도달이면 real 에 없어 basis 0 → 재매수가 정당(원주문 미존재). 즉 새 uuid 라도 BUY 더블체결은 pending+reconcile 로 차단됨. SELL network-error 의 위험은 finding 2 가 별도 커버. API 계약 결함(401/네트워크 분류·멱등키 비결정성)은 실재하나, 본 finding 이 든 '더블체결' 귀결은 BUY 에서 mitigated → partial. | alt:코드 일치: 읽기타임아웃/연결끊김(requests.RequestException)은 max_retries 소진 후 raise TossAPIError('network-error', str(e), None, None)(toss.py:120-124), http_status=None → is_business_error()=False(line 59-60) → place_order 에서 4xx 만 REJECTED 흡수하고 그 외 raise(261-263) → network-error 는 raise 경로. live_engine.py:208-216 은 raise 시 해당 주문 orders 미기록·record_error 누적 → 다음 실행 동일 비중 재plan. clientOrderId=uuid.uuid4().hex[:32] 가 매 place_order 호출마다 새로 생성(toss.py:246)되므로 '응답만 못 받은 접수성공' 주문을 다음 실행에서 다른 멱등키로 재제출 → 더블체결 위험 실재. _request 내부 재시도는 동일 body 라 멱등이지만 place_order 재호출 경로는 미보호. 결함 실재(KMI 1주 규모라도 멱등키 설계결함 자체는 유효).
- [Major] run_exit.py:_run_locked (라인 86-99) / live_exit.py:check_exits (라인 52) | 청산 경로에 데이터 신선도 게이트가 전혀 없음 — stale 일봉으로 자동매도 가능 → **confirm**  lens[confirm/confirm] reproduce:재현됨. 진입 경로 live_engine.py:151-157 은 session_gap(last_bar, today)>max_staleness_sessions 면 status='stale' 거부. 청산 경로 run_exit.py 는 session_gap import 자체가 없음(run_exit.py:21 은 last_completed_session 만 import). _run_locked(86-99)는 data.load 로 종가만 받고 신선도 미검사. check_exits(live_exit.py:52)는 price is None or s is None or len(s)<regime_ma(봉 개수)만 검사 — 일봉 신선도 미검사. 따라서 며칠/주말 이월 stale 일봉으로 _sma(s,200)/_sma(s,50) 계산 후 실시간가와 비교해 200MA이탈/손절 트리거 발화 가능 → 잘못된 MA 레벨로 봇 보유분 자동청산. 진입/청산 신선도 정책 비대칭 확인. | refute:반증 실패 — 결함 실재. run_exit.py 는 calendar_util 에서 last_completed_session 만 import(라인 21) — session_gap import·호출 부재 확인. 청산 데이터 적재(run_exit.py:90 data.load)·check_exits(live_exit.py:52)는 price is None or s is None or len(s)<regime_ma 만 검사 — 일봉 신선도(session_gap) 미검증. 반면 진입경로 live_engine.py:151-157 은 session_gap>max_staleness_sessions 면 status=stale 거부. market_open('US') 게이트(run_exit.py:70)는 '지금 정규장 시간'만 보장할 뿐 yfinance 일봉의 다세션 지연(피드랙·주말이월)을 막지 못함 → stale 종가로 _sma(s,200/50) 계산·실시간가와 비교해 200MA/손절 오발화 가능. 청산은 매도(위험축소) 방향이라 손실보다는 '잘못된 MA 레벨로 조기/허위 청산' 결함. live_exit.py:13 주석은 일봉MA+실시간가를 설계전제로 두나 일봉 자체 신선도 게이트가 코드 어디에도 없음 — confirm.
- [Minor] broker/managed.py:177 (get_account cap) + broker/executor.py:38,41,64 (사이징 기준가) | cash_cap 은 사이징시점 명목캡일 뿐 실지출캡 아님 — 슬리피지·수수료 초과 가능 (체결 후 대조 부재) → **confirm**  lens[confirm/confirm/confirm] reproduce:재현됨. managed.py:177 cash=min(real.cash,cash_cap) 은 사이징 입력만 제한. executor.py:38 price=get_quote().last, :41 tgt_qty=int(w*investable/price), :64 unit=price*buy_mult(라이브 1.0), afford=int(budget/unit) 으로 동일 명목시세 사용. toss.py:243-265 place_order 는 자체 현금검사·수수료반영 없이 MARKET 미지체결가로 채워짐. cash_cap 은 grep 상 managed.py:177 사이징 외 경로 없음 — 체결 후 실지출 대조 부재. 실계좌현금 ~$1,167≫$100 이라 토스가 소액 초과를 거부 안 함 → 슬리피지+수수료만큼 cash_cap 초과지출 가능. buy_mult=1.0 헤드룸 부재와 연계. KMI 1~3주 규모에선 미미하나 '$100 한도 정확'은 엄밀 미충족. Minor 타당. | refute:반증 실패 — 결함 실재(Minor). managed.py:177 cash=min(real.cash,cash_cap) 는 사이징 입력만 캡. executor.py:38 price=get_quote().last, 41 tgt_qty=int(w*investable/price), 63-64 unit=price*buy_mult(라이브=1.0)·afford=int(budget/unit) — 모두 명목 시세 기준. toss MARKET 은 미지 실체결가로 채워지고(toss.py:243-265 place_order 자체 현금/수수료검사 없음 — 서버 위임), 체결 후 실지출을 cash_cap 과 대조하는 경로 부재(get_account 외 cash_cap 사용처 없음). 실계좌현금~$1,167≫$100 라 토스가 소액 초과를 거부 안 하므로 슬리피지+수수료만큼 cash_cap 초과지출 가능. buy_mult=1.0 헤드룸 부재(finding 1)와 연계된 root 일치. KMI 1~3주 규모에선 초과 미미 — 결함 실재·영향 경미 confirm. | alt:Minor 로 정확: cash_cap 은 get_account().cash=min(real.cash, cash_cap)(managed.py:177)로 사이징 입력만 제한. tgt_qty=int(w*investable/price)(executor.py:41), unit=price*buy_mult, afford=int(budget/unit)(63-64) 모두 quote.last 명목가 기준이고 라이브 buy_mult=1.0(헤드룸 0). toss place_order(toss.py:243-265)는 자체 현금검사·수수료반영 없이 서버위임, MARKET 실체결가 미지. 체결 후 실지출을 cash_cap 과 대조하는 경로 코드 전무. real.cash ~$1,167 ≫ $100 이라 토스가 소액초과 거부 안 함 → 슬리피지+수수료만큼 cash_cap 초과지출 가능. KMI 1~3주 규모서 초과 미미하나 '$100 한도 내 정확동작' 엄밀충족 못 함은 사실. buy_mult Major 와 연계 근본원인 정확. 결함 실재(Minor).
- [Minor] broker/managed.py:place_order(195-200) vs record_fills(219-225) | BUY 제출이 5xx/네트워크로 raise되면 pending은 영속되나 record_fills 미실행 — force 재실행 시 재매수 가능 → **confirm**  lens[confirm/partial/confirm] reproduce:재현됨. managed.py:198-199 _pending 영속·_save 가 :200 self._broker.place_order 보다 선행. toss.py:263 5xx/네트워크 raise 시 live_engine.py:208-209 for 루프가 예외로 빠져 record_fills(:223-228)·mark_traded(:245) 미도달, status=error 반환(:213-216). pending 에 의도 잔존, basis 미갱신. 안전성은 다음 실행 reconcile_basis(managed.py:250 min(real, managed+pending) cap)에 의존하나 live_engine.py:161-166 reconcile_basis 는 실행 시작 시 1회만. run_live.py:150·158 --force 가 already_traded(live_engine.py:140) 우회 → force 재실행 시 reconcile_basis 가 먼저 돌되 토스 도달주문이 real 미반영(정산지연)이면 pending 흡수 못 함 → Executor diff 재계산 → 재매수 가능. 라인 근거 일치. Minor 타당. | refute:메커니즘 confirm, 'force 재실행 더블매수' 귀결은 대부분 반증됨. 사실: managed.py:198-200 이 _save(pending) 를 submit 보다 선행, toss.py:263 5xx/네트워크 raise, live_engine.py:208-216 예외 시 record_fills(226)·mark_traded(245) 미도달·status=error — 확인. 그러나 force 재실행도 _run_once_locked 의 reconcile_basis(live_engine.py:161-166)를 plan 보다 먼저 통과한다(force 가 우회하는 것은 already_traded(140)뿐, reconcile 아님). reconcile_basis(managed.py:249-255): 주문이 실제 체결됐으면 real 에 잡혀 basis=min(real,basis+pending) 로 흡수→Executor diff≈0→재매수 안 함; 미도달이면 real=0→basis 0→재매수 정당. 즉 두 경우 모두 더블매수 차단. 잔여 위험은 finding 본문도 인정하는 '체결됐으나 토스 holdings 미정산' 결제지연 윈도우에서 real 이 아직 0 으로 보이는 협소한 레이스뿐 — 일반적 더블매수 아님. '경고 알림 부재'는 운영 개선점이나 실질 더블매수 귀결은 mitigated → partial. | alt:경로 확인: place_order 는 _pending 영속·_save(198-199) '후' self._broker.place_order(200) 호출. 토스 5xx/네트워크 raise(toss.py:263) 시 live_engine for 루프(208-209)가 예외로 빠져 record_fills(223-228)·mark_traded(245) 미도달, status=error 반환(213-216). pending 잔존, managed 미갱신. 다음 실행 reconcile_basis(live_engine.py:161-166, 실행시작 1회)는 min(real, managed+pending)(managed.py:250)로 cap — 주문이 토스에 도달했으나 아직 real positions 에 미반영(비동기 체결지연)이면 newb=min(real, basis+pending) 가 흡수 못 하고 pending 비움(255) → Executor 가 diff 재계산 → 이미 도달한 주문과 별개 재매수 가능. --force 가 already_traded 우회(run_live.py:150·140). 동일프로세스 즉시복구 경로 없음. benign(체결됐으면 real 에 잡혀 cap 흡수)은 체결지연/미반영 윈도우엔 불성립. 결함 실재(Minor).
- [Minor] broker/toss.py:153-169, _ensure_connected / connect 토큰 만료 | connect()는 만료 직전 자동갱신하나 _request 도중 401 만료 시 재인증 없이 REJECTED 오분류 → **confirm**  lens[confirm/confirm/confirm] reproduce:재현됨. toss.py:154-156 _ensure_connected 는 메서드 진입 시점에만 토큰 만료검사. :168-169 60초 마진도 진입 시점 기준. 진입 후 HTTP 호출 사이 만료 시 401 응답 → _request(toss.py:140-150)는 4xx(429/5xx 아님)로 즉시 raise, 자동 재인증 없음. place_order(:261-262) is_business_error()=True(401∈[400,500)) → REJECTED Order 흡수 → 실제 토큰만료를 주문거부로 오보고. 60초 마진 덕에 통상 안전하나 장시간 폴링·시계드리프트 경계에서 오분류 여지 확인. Minor 타당. | refute:반증 실패 — 결함 실재(Minor). _ensure_connected(toss.py:154-156)는 method 진입 시점만 만료검사·60초 마진(169). 진입 후 HTTP 호출 사이 토큰 만료 시 401 응답은 _request 가 4xx 즉시 raise(toss.py:140-150, 401 재인증/재시도 경로 없음). place_order 에서 401 은 is_business_error()=True(toss.py:59-60, 400≤status<500)→REJECTED 흡수(261-262) → 실제 '토큰만료'가 '주문거부'로 오보고. 60초 마진·10s timeout·max_retries 2 덕에 통상 안전하나, _await_fills 장시간 폴링(get_order 반복)·시계 드리프트 경계서 401 오분류 윈도우 실재. 미체결을 거부로 기록하는 정합성 결함 confirm(영향 경미). | alt:확인: _ensure_connected(153-156)는 메서드 진입시점만 만료검사, 60초 전 갱신여유(168-169)도 진입기준. 진입 후 HTTP 호출 사이 토큰만료 시 401 응답은 _request 에서 4xx → 즉시 raise(140-150), 자동 재인증/재시도 없음. place_order 경로 401 은 is_business_error()=True(59-60) → REJECTED Order 흡수(261-262)되어 '토큰만료'가 '주문거부'로 오보고 → 미체결을 거부로 오인. 60초 마진으로 통상 안전하나 장시간 폴링·시계드리프트 경계서 오분류 여지 실재. Minor 로 정확.
- [Minor] broker/toss.py:279, get_order _STATUS_MAP 미지정 기본값 | 미지정 토스 상태코드가 SUBMITTED로 폴백 — 종료상태 누락 시 무한폴링 → **confirm**  lens[confirm/confirm/confirm] reproduce:재현됨. toss.py:279 _STATUS_MAP.get(status, OrderStatus.SUBMITTED) 로 미지정 상태→SUBMITTED 폴백. _STATUS_MAP(toss.py:34-45)에 EXPIRED·DONE_FOR_DAY 등 DAY 주문 마감 종료상태 없음. 그런 상태 수신 시 live_engine.py:60 _TERMINAL={FILLED,REJECTED,CANCELLED} 가 SUBMITTED 미포함 → _await_fills(live_engine.py:72-85)가 종료 미인식, fill_timeout(기본 30s)까지 폴링 후 partial 보고. 자금손실 직결 아니나 마감 미체결 즉시 종료 미인식으로 30s 낭비·partial 오버리포팅 확인. Minor 타당. | refute:반증 실패 — 결함 실재(Minor). toss.py:279 get_order 는 _STATUS_MAP.get(status, OrderStatus.SUBMITTED) — 미지정 상태를 SUBMITTED(폴링 지속)로 폴백. _STATUS_MAP(34-45)에 EXPIRED/DONE_FOR_DAY 등 DAY 주문 마감 종료상태 부재 확인. live_engine.py:60 _TERMINAL={FILLED,REJECTED,CANCELLED} 만 종료이므로 그런 상태가 오면 _await_fills(72-85)가 종료 인식 못해 fill_timeout(30s)까지 폴링 후 partial 보고. 자금손실 직결 아니나(체결 오인 아님) 마감 미체결 즉시인식 실패→30s 낭비·partial 오버리포팅 실재. 토스가 실제 그 상태를 반환하는지는 스펙 미확인이나 '미지정→SUBMITTED 무한폴링' 폴백 동작 자체는 코드상 실재 — confirm. | alt:확인: get_order 는 _STATUS_MAP.get(o.get('status'), OrderStatus.SUBMITTED)(toss.py:279). _STATUS_MAP(34-45)에 EXPIRED·DONE_FOR_DAY 등 DAY 주문 마감 종료상태 미포함. 그런 상태 수신 시 _await_fills(live_engine.py:63-85)가 _TERMINAL(60: FILLED/REJECTED/CANCELLED)로 종료 미인식 → fill_timeout(30s)까지 폴링 후 partial 보고. 자금손실 직결 아니나(체결 오인 없음) 마감 미체결 즉시종료 미인식 → 30s 낭비·partial 오버리포팅 실재. finding 도 'timeout 까지' 로 한정(무한 아님)해 정확. 결함 실재(Minor).
- [Minor] broker/toss.py:217-235, market_open 시간대 비교 | market_open이 캘린더 tz와 로컬 tz 혼합 비교 — naive 응답 시 TypeError→휴장 폴백으로 청산 영구보류 → **confirm**  lens[confirm/confirm/partial] reproduce:재현됨. toss.py:230-231 datetime.fromisoformat(rm['startTime'/'endTime']), :232 now=datetime.now().astimezone()(aware). 토스가 tz 미포함(naive) ISO 반환 시 start/end naive·now aware → start<=now<=end 가 TypeError. except (ValueError, TypeError)(:234-235) 가 잡아 False(휴장) 반환 → run_exit.py:70-71 market_open False → status=closed 청산 보류. 안전측(매도 안 함)이나 정규장 중에도 손절/200MA 청산이 영구 보류돼 리스크축소 미실행. tz 미포함 응답 조건부지만 코드상 재현 가능. Minor 타당. | refute:반증 실패 — 조건부 결함 실재(Minor). toss.py:230-231 fromisoformat(rm['startTime'/'endTime']) vs 232 now=datetime.now().astimezone()(tz-aware). 토스가 tz 미포함(naive) ISO 문자열을 반환하면 start/end 는 naive·now 는 aware → 233 'start<=now<=end' 가 TypeError, 234-235 except (ValueError,TypeError)→return False(휴장). run_exit.py:70-71 market_open False→status=closed 청산 보류. 즉 정규장 중에도 MARKET 청산이 영구 'closed' 로 보류돼 손절/200MA 리스크축소가 실행 안 됨. 안전측(허위 매도 없음)이나 위험축소 미실행은 정합성 결함. 발화는 토스 응답이 naive ISO 일 때만 — 토스 실제 포맷은 스펙 미확인이라 조건부지만, 코드의 tz 혼합비교·TypeError→closed 폴백 경로는 실재 confirm. | alt:코드 결함 자체는 실재: now=datetime.now().astimezone()(toss.py:232, 항상 tz-aware) vs start/end=datetime.fromisoformat(rm['startTime'/'endTime'])(230-231)을 '<=' 비교. fromisoformat 은 입력 문자열에 tz 가 없으면 naive 를 반환하므로, 토스가 naive ISO 를 주면 aware↔naive 비교 → TypeError → except(234-235) False(휴장) → run_exit.py:70-71 status=closed 청산 영구보류. 그러나 '영구보류' 결론은 토스가 실제로 naive 문자열을 반환한다는 전제에 의존하며, 이 전제는 제공 입력(toss.py 코드)만으로 검증 불가 — 시장캘린더 API 가 tz-aware ISO(예 '...+09:00')를 반환하면 aware-aware 비교 성립해 결함 미발현. finding 자신도 'naive 응답 시'로 조건부 한정. 따라서 코드 fragility 는 confirm 하되 실해(영구보류) 발현은 미검증 외부계약 의존 → partial.
- [Minor] live_engine.py:181-184 (_run_once_locked) / live_risk.py:18-36 | vol_target=0 이면 레짐(SPY 200MA) 필터까지 통째로 우회됨 → **confirm**  lens[confirm/confirm/confirm] reproduce:재현됨. live_engine.py:181-184 risk={}; if cfg.vol_target > 0: weights, risk = apply_overlay(...) — vol_target>0 일 때만 apply_overlay 호출. apply_overlay(live_risk.py:18-36)는 레짐필터(SPY<200MA→{} 전량현금)와 vol_target 을 함께 포함. 따라서 cfg.vol_target=0 설정 시 레짐필터까지 동시 비활성 → SPY 200MA 하회에도 정상 매수 지속. live_engine.py:36 기본 vol_target=0.20(>0)이라 현 KMI 경로는 안전하나 vol_target=0 설정 시 약세장 매수로 조용히 무력화. 두 독립통제가 하나의 게이트에 결합된 결함 확인. Minor 타당. | refute:반증 실패 — 결합 결함 실재(Minor·잠재). live_engine.py:181-184 risk={}; if cfg.vol_target>0: weights,risk=apply_overlay(...). apply_overlay(live_risk.py:14-51) 내부에 레짐필터(18-36: SPY<200MA→{} 전량현금)와 변동성타겟(38-51)이 함께 위치 — 레짐 판정 단독 호출경로 없음(grep 상 apply_overlay 가 유일 진입). 따라서 vol_target=0 설정 시 변동성 사이징뿐 아니라 베어마켓 현금화 레짐필터까지 통째 우회 → SPY 200일선 아래여도 정상 매수 지속(자금손실 방향). 두 독립 통제가 단일 게이트에 결합. 운영기본 vol_target=0.20(>0, live_engine.py:35)이라 현 KMI 검증경로는 안전 — 향후 vol_target=0 시 조용히 무력화. 결함 실재·현재 비활성 confirm. | alt:확인: live_engine.py:181-184 는 risk={} 후 if cfg.vol_target > 0: weights,risk=apply_overlay(...) 로만 오버레이 호출. apply_overlay(live_risk.py:14-51) 내부에 레짐필터(18-36: SPY<200MA → return {},info 전량현금)와 vol target 이 함께 위치. 따라서 vol_target=0 설정 시 레짐필터까지 동시 비활성 → 베어마켓 현금화 보호 무력, SPY 200일선 아래여도 정상매수 지속. 두 독립통제(vol vs regime)가 단일 조건에 결합된 결함 실재. 운영기본 vol_target=0.20>0(live_engine.py:36)이라 현 KMI 경로는 안전하나, 향후 vol_target=0 설정 시 자금손실방향(약세장 매수) 무력화 — finding 도 동일 한정. 결함 실재(Minor, 잠복).
- [Minor] run_exit.py:run (라인 52-53) + run_exit.py:_run_locked (라인 86) | run_exit 가 ET 세션 대신 로컬(KST) 날짜로 fail-open·end_excl 산출 — 킬스위치 baseline 키 오염 및 세션경계 off-by-one → **partial**  lens[confirm/partial] reproduce:재현됨. (a) run_exit.py:52-53 session=last_completed_session(); today=(session or datetime.now().date()).isoformat() — calendar_util.py:41-42 docstring상 None=비정상인데 청산은 KST datetime.now().date() 로 조용히 폴백(진입 run_live.py:96-98 은 None 이면 error 거부). today 는 KillSwitch(today=today)(run_exit.py:72) baseline·resume_if_new_day 키 → 비정상 캘린더에서 ET 세션 아닌 KST 날짜가 baseline 키. (b) run_exit.py:86 end_excl=(datetime.now().date()+timedelta(days=1)) 도 KST '오늘' 기준, 진입 run_live.py:100 (session+timedelta(days=1)) 대비 KST 자정~05:00 구간 end_excl 이 ET 세션보다 하루 앞서 off-by-one. calendar_util.py:22-24 now_et 제공하나 run_exit.py:52-86 미사용. 두 sub-issue 모두 재현. Minor 타당. | refute:코드 divergence 는 실재하나 'baseline 상시 오염' 프레이밍은 과장. (a) run_exit.py:52-53 'session or datetime.now().date()' — last_completed_session()==None(calendar_util.py:41-42 비정상=최근12일 세션無) 일 때만 KST now().date() 폴백, 진입경로 run_live.py:96-98 은 None 거부. 즉 KST 오염은 None 비정상 케이스에 한정 — 정상 케이스는 today=session.isoformat()=ET 세션일로 진입경로와 동일(KillSwitch 동일 STATE_FILE 공유 시 정합). finding 의 'ET 세션일과 KST 날짜가 상시 1일 어긋남→baseline 정합 어긋남' 은 정상경로엔 부적용(과장). (b) run_exit.py:86 end_excl=datetime.now().date()+1(KST) vs run_live.py:100 session+1(ET) — KST 자정~05:00 US 세션 구간서 end_excl 이 ET 세션+1 보다 하루 앞섬(off-by-one) 실재하나 yfinance 미래봉 무시로 실해 없음(finding 도 인정). None-폴백 divergence 와 off-by-one 은 실재 결함이나, 헤드라인 'baseline 키 오염' 은 비정상 None 케이스 한정·실해 negligible → partial.